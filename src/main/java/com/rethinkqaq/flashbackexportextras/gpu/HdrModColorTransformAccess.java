package com.rethinkqaq.flashbackexportextras.gpu;

/** Implemented on HDR Mod's 26.1 color-transform renderer by an optional Mixin. */
public interface HdrModColorTransformAccess {
    void flashbackplus$configureOutput(int textureFormat, int readPixelFormat);
}
